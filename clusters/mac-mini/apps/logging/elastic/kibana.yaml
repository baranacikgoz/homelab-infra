apiVersion: kibana.k8s.elastic.co/v1
kind: Kibana
metadata:
  name: main-kibana
  namespace: logging
spec:
  version: 8.17.0
  count: 1
  elasticsearchRef:
    name: main-cluster
  podTemplate:
    spec:
      containers:
        - name: kibana
          resources:
            requests:
              memory: 256Mi
              cpu: 500m
            limits:
              memory: 1.5Gi # Tampon payı
