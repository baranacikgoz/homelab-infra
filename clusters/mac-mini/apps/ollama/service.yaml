apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: ollama
spec:
  type: ExternalName
  externalName: host.orb.internal
  # Note: ExternalName services do not use selectors.
  # Pods will resolve 'ollama.ollama.svc.cluster.local' to the host IP.
