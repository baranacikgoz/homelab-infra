# Scale Set Configuration for the Claude Flutter Developer
# Aligned with T-Shirt Medium and Governance Anti-OOM

gha-runner-scale-set:
  githubConfigUrl: "https://github.com/baranacikgoz/modular-monolith-ddd-vsa-webapi"
  githubConfigSecret: arc-github-app-secret
  
  # Cross-namespace Controller Discovery
  controllerServiceAccount:
    namespace: arc-system
    name: arc-controller-gha-rs-controller
  
  # Safety boundaries for Mac Mini M4 resource pool
  minRunners: 0
  maxRunners: 2
  
  # Service account to execute high-privilege build actions if needed
  # Default is fine for now
  
  template:
    spec:
      containers:
        - name: runner
          image: ghcr.io/baranacikgoz/homelab-infra/claude-flutter-developer:latest
          imagePullPolicy: Always
          command: ["/home/runner/run.sh"]
          # Performance Mode: 6.0 CPU Burst / 2.5Gi RAM
          resources:
            requests:
              cpu: 1500m
              memory: 1Gi
            limits:
              cpu: 6000m
              memory: 2.5Gi
          env:
            - name: RUNNER_TAGS
              value: "claude-ai,flutter-dev"
            - name: DOCKER_HOST
              value: tcp://127.0.0.1:2375
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: claude-ai-secret
                  key: anthropic_api_key
                  optional: true
          volumeMounts:
            - name: diag-logs
              mountPath: /home/runner/_diag
        - name: dind
          image: docker:27-dind
          # Small variant for DIND overhead
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 512Mi
          securityContext:
            privileged: true
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
          volumeMounts:
            - name: docker-graph-storage
              mountPath: /var/lib/docker
        - name: observability
          image: alpine:latest
          command: ["/bin/sh", "-c", "touch /home/runner/_diag/claude.log && tail -f /home/runner/_diag/claude.log"]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          volumeMounts:
            - name: diag-logs
              mountPath: /home/runner/_diag
      volumes:
        - name: docker-graph-storage
          emptyDir: {}
        - name: diag-logs
          emptyDir: {}
